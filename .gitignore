# 建置產物 (CMake) — 各專案 build/ 皆在 projects/*/build/（build*/ 無斜線，任一層皆命中）
build*/
projects/godot_zone/bin/

# 編譯中間檔 / 二進位
*.o
*.a
*.so
*.dylib
*.dll
*.lib
*.obj
*.pdb

# Godot
.godot/
*.gd.uid
*.gdextension.uid

# 存檔目錄 (user://)
user:/

# OS
.DS_Store
Thumbs.db
