# 架構與函數說明

> 最後更新：2026-07-10
> 這份說明「整個程式怎麼搭起來的」＋「每個檔案/函數在做什麼」。
> 想看白話進度看 `進度說明.md`；想看建置指令看 `README.md`。

---

## 1. 三層鳥瞰：誰呼叫誰

```
   玩家按鍵 (Godot)
        │
        ▼
 ┌─────────────────────┐   godot_zone/  (GDScript：畫面、按鍵)
 │  map_view.gd        │   按一鍵 → 呼叫 ZoneWorld 的方法
 └─────────┬───────────┘
           │  player_move / player_wait / next_round
           ▼
 ┌─────────────────────┐   src/gbind/  (C++ ↔ Godot 橋樑)
 │  ZoneWorld          │   把 Godot 呼叫翻譯成核心邏輯；事件翻成 Godot signal
 └─────────┬───────────┘
           │  使用
           ▼
 ┌─────────────────────┐   src/core/  (純 C++，不認識 Godot)
 │  TurnEngine         │   回合排程
 │  apply_action       │   把動作結算到世界
 │  ECS / 元件 / 系統   │   資料與規則
 └─────────────────────┘
```

**重點原則**：`src/core/` 完全不依賴 Godot（純邏輯、可單元測試）；只有 `src/gbind/` 認識 Godot。

---

## 2. 一回合裡發生什麼（資料流）

玩家按一下方向鍵，整條鏈是這樣跑的：

1. `map_view.gd` 收到按鍵 → 呼叫 `ZoneWorld::player_move(dx, dy)`。
2. `ZoneWorld` 把這個指令 `submit()` 給 `TurnEngine` 裡正在等待的玩家 actor。
3. `ZoneWorld::pump()` 反覆呼叫 `engine_.step()`：
   - 玩家 actor → 用剛投遞的指令 `resolve()`（最終呼叫 `apply_action`）。
   - 每個 NPC → `decide()` 決策（`decide_npc`，見 3.1）→ `resolve()` → `apply_action`。
4. `apply_action` 改動世界（移動/攻擊/撿道具…），把發生的事丟進 `events_`。
5. `ZoneWorld::drain_events()` 把事件翻成 Godot signal，前端據此更新畫面。
6. 一輪所有 actor 跑完 → `RoundDone`，發 `round_finished` signal。

**回合制的精髓**：按一鍵 ＝ 英雄走一步 ＋ 全部 NPC 各動一次。

---

## 3. 核心層 `src/core/`

### 3.1 回合引擎 `turn/`

#### `turn_engine.{h,cpp}` — `TurnEngine`
回合排程的心臟。內部就是一條 actor 的 `std::list`（順序＝加入順序，hero 最先）。

| 函數 | 做什麼 |
|------|--------|
| `add_actor(e)` | 新 actor 出現 → 接到清單尾端（重複則略過）。 |
| `remove_actor(e)` | 死亡/離場 → 從清單移除；若剛好輪到它，cursor 安全前移避免懸空。 |
| `contains(e)` / `size()` / `order()` | 查詢清單。 |
| `clear()` | 清空整條清單與狀態。 |
| `begin_round()` | 開新一輪：cursor 回到清單頭。 |
| `step(ctx)` | **核心**：推進一個 actor。回傳 `Acted`（動完可再 step）/ `WaitingPlayer`（卡在玩家等指令）/ `RoundDone`（整輪跑完）。每個 actor 首次輪到時把 `ActPointComponent` 設 1000，動完歸 0。 |
| `submit(e, a)` | 玩家把指令投遞進來（存進 `pending_`）。 |
| `waiting_actor()` | 目前卡在哪個 actor 等指令。 |

> `EngineCtx`：傳給 `step()` 的依賴包（`registry` ＋ `decide`/`resolve`/`trace` 三個 callback）。引擎本身不持有 registry，保持純粹好測試。

#### `action.h` — `Action`
一個「指令」的值型別，玩家和 NPC 共用。只有三種：`Idle` / `Move` / `Wait`。
**沒有獨立攻擊動作**——攻擊＝移動進入有 actor 的格子。
- `Action::idle()` / `wait()` / `move(dir)` — 建構各種動作。

#### `apply_action.{h,cpp}` — 動作結算
把一個 `Action` 真正落地到世界上。

| 函數 | 做什麼 |
|------|--------|
| `apply_action(reg, map, self, a, events)` | 結算一個動作：撞牆（發 `BumpedWall`）/ 撞 actor（攻擊，發 `BumpedActor` 或 `ActorDied`）/ 移動（順帶自動撿道具、踩到下樓梯發 `ReachedStairDown`）。死亡目標只發事件、不在這裡 destroy（交呼叫端統一移除，避免迭代失效）。 |
| `actor_at(reg, x, y, except)` | 查 `(x,y)` 站著哪個別的 actor（`except` 排除自己）；沒有則回 `entt::null`。公開在標頭，撞擊攻擊判定與 `decide_npc` 的尋路避讓共用。 |

#### `npc_ai.{h,cpp}` — `decide_npc`：回合引擎唯一的 NPC 決策函式
純決策函式：只讀世界、只回傳一個 `Action`，唯一副作用是更新自己的 `NpcAiComponent` 警覺狀態。世界的實際改動一律交回 `apply_action()`。取代了舊的兩套並存 AI——`apply_action.cpp` 內的 `decide_chase`（回合引擎原本實際用的最簡追擊）與 `src/core/systems/npc_ai_system.*`（另一套世界改動型群體 AI，**已刪除**）。

`decide_npc(reg, map, self, target, rng, cfg = {})`，`NpcAiConfig{ sight_radius=10, alert_memory=6 }`，決策順序：

1. **更新警覺**（每回合必做，不受 `move_chance` 閘門）：目標在 `sight_radius`（Chebyshev 距離）內且通過 `los()` → `alerted=true`、`alert_turns` 重置為 `alert_memory`；否則若已警覺，`alert_turns` 遞減，歸零則解除警覺。
2. **已警覺且與目標相鄰** → 朝目標 Move（＝撞擊攻擊）。這一步不受 `move_chance` 閘門——否則低 `move_chance` 的怪在貼身戰裡常常一次都還不了手。
3. **`move_chance` 擲骰失敗** → `Wait`。此閘門只管「移動」（追擊／徘徊）的頻率，不管攻擊。
4. **已警覺 → 追擊**：大 delta 軸優先朝目標移動，該軸被牆或其他 actor 擋住就換另一軸；兩軸都不可走則落到徘徊，不會對著牆空轉。
5. **未警覺 → 徘徊**：隨機起點掃四方向，找得到可走格就走，四方向都不可走（含被其他 actor 佔滿）則 `Wait`——不會誤擠進同類佔的格子。

#### `zone_event.h` — `ZoneEvent`
核心層發出的遊戲事件（核心不認識 Godot，只丟事件）。種類：`BumpedWall` / `BumpedActor` / `ActorDied` / `ItemPickedUp` / `ReachedStairDown`。

#### `move_dir.h` — 方向編碼
- `encode_dir(dx, dy)` / `decode_dir(p, dx, dy)` — 把 8 方向 `(dx,dy)` 壓成一個 0..8 整數（存在 `Action.param`）來回轉換。

### 3.2 元件 `components/`（ECS 的「資料」）

| 元件 | 內容 / 用途 |
|------|-------------|
| `ActorComponent` | 空 tag，標記「有行動資格」（英雄、NPC）。物品/地形不掛。 |
| `ControllerComponent` | 這個 actor 由誰操控：`Player`（暫停等玩家）/ `Self`（AI 決策）/ `Faction`（軍團，未實作，暫等同 Self）。 |
| `ActPointComponent` | 行動點數，每回合重設 1000、動完歸 0。為未來「依成本多次行動」預留。 |
| `HeroComponent` | 空 tag，正向標記唯一辨識英雄（取代脆弱的排除法）。 |
| `SpatialComponent` | 座標 `x,y` ＋ `parent`（場景圖父子；目前多為頂層）。 |
| `HealthComponent` | `hp` / `max_hp`，預設 10/10。 |
| `CombatStatsComponent` | `attack`（攻擊力，預設 2）、`move_chance`（移動機率）。 |
| `NpcAiComponent` | NPC AI 狀態：`alerted`（是否發現英雄）、`alert_turns`（失去視線後追幾回合）、`is_caster`（註：技能已移除，此欄為歷史殘留）。 |
| `ItemComponent` | 道具：`type`（目前只有 `health_potion`）、`value`（回血量）。 |
| `WorldStateComponent` | 全域狀態：`turn_count`（回合數）、`current_floor`（樓層）。掛在地圖實體上，隨存檔一起存。 |

### 3.3 系統 `systems/`（ECS 的「規則」）

#### `fov_system.{h,cpp}` — 視野
- `compute_fov(map, ox, oy, radius)` — 用 Bresenham 射線算英雄視野，更新 `visible`（本回合）＋ `explored`（霧中戰爭）。
- `los(map, x0,y0, x1,y1)` — 兩點間視線檢查（`decide_npc()` 判斷能否看到目標用），不改地圖狀態。

> 舊的 `npc_ai_system.{h,cpp}`（世界改動型群體 AI，與 `decide_chase` 平行存在的另一套）已刪除；NPC 決策現在唯一走 `turn/npc_ai.cpp` 的 `decide_npc()`（見 3.1）。

### 3.4 地圖 `maps/`

| 檔案 | 內容 |
|------|------|
| `tile.h` — `Tile` | 單格：`terrain` id ＋ `flags`（可走/擋視線/下樓梯）。提供 `is_walkable()` 等查詢。 |
| `map_data.h` — `MapData` | 整張地圖（稠密 tile 網格，列優先索引）。一張地圖是一個元件掛在「地圖實體」上，不是每格一個實體。提供 `at()` / `in_bounds()` / FOV 可見性查詢；`tiles` 存檔、`visible/explored` 不存（load 後重置）。 |
| `map_gen.{h,cpp}` | `generate_bsp_dungeon(map, rng)` — BSP 地下城生成：整張填牆，再挖房間＋走廊，回傳房間清單（第 0 間＝英雄起始房）。 |

### 3.5 ECS 底層 `ecs/`

- `entity_manager.h` — `EntityManager`：`entt::registry` 的薄封裝。`create/destroy/valid`、`emplace/get/try_get/has/remove` 元件、`add_system/tick` 跑系統。複雜查詢直接 `registry()`。
- `system_ctx.h` — `SystemCtx`：傳給每個系統的依賴包，目前是空佔位（未來放 rng、locale 等）。

### 3.6 序列化 `serialize/`（存讀檔）

- `all_components.h` — `AllComponents`：所有「要存檔的元件」清單（**唯一真相來源**）。加新元件要存檔就改這裡。
- `save_load.h` — `save()` / `load()`：把整個 registry 與 bytes/檔案/SaveStore 來回轉換。三層 API（stream / 檔案路徑 / SaveStore）。
- `entt_cereal_archive.h` / `save_store.h` — cereal 與 EnTT snapshot 的轉接、儲存後端介面。

---

## 4. 橋樑層 `src/gbind/` — `ZoneWorld`

唯一認識 Godot 的地方。對 GDScript 暴露的 Node，內部持有 `EntityManager em_` ＋ `TurnEngine engine_`。

**對前端公開的方法**（GDScript 可直接呼叫）：

| 方法 | 做什麼 |
|------|--------|
| `next_round()` | 「下一回合」：開新一輪並推進到「玩家要等指令」或「回合結束」。 |
| `player_move(dx, dy)` | 玩家移動指令；投遞給等待中的玩家 actor 並續跑該回合。 |
| `player_wait()` | 玩家原地等待。 |
| `is_waiting_player()` | 現在是否卡在玩家 actor 等指令。 |
| `round_in_progress()` | 這回合還沒跑完？ |
| `get_map_width/height()`、`is_walkable(x,y)` | 地圖尺寸與可走查詢。 |
| `get_tile_flags()` | 整張地圖旗標一次過河（`PackedByteArray`，row-major `y*width+x`）：bit0 可走／bit1 下樓梯／bit2 本回合可見／bit3 曾探索。**沒有 `generate_map_image()`——渲染完全交給 `godot_zone/dungeon_view.gd`，核心/橋樑層只吐資料不做圖形決策。** |
| `get_visible_entities()` | `Array[Dictionary]`，鍵 `x,y,kind,hp,max_hp`，`kind ∈ {"hero","npc","item"}`；只含視野內實體（英雄恆含），繪製序＝陣列序（英雄最後）。 |
| `get_hero_x/y`、`get_hero_hp/max_hp`、`get_hero_attack()`、`get_turn_count`、`get_npc_count`、`get_current_floor`、`get_win_floor()`、`is_game_over()`、`is_game_won()` | 狀態查詢（給 HUD／結局判斷）。`get_win_floor()` 回傳走下該層樓梯即獲勝的樓層數。 |
| `restart()` | 重開一局；若曾呼叫過 `set_seed()` 則沿用同一種子重跑，否則抽新種子。 |
| `set_seed(seed)` / `get_seed()` | 固定/查詢共用的 `std::mt19937` 種子——同一種子重現整場（地圖生成＋NPC AI 都吃同一顆種子）。headless `verify.gd` 的機器人驗證仰賴此。 |
| `save_game/load_game/has_save_game(path)` | 存讀檔。 |
| `get_debug_text/log()`、`set/get_trace_enabled()`、`clear_debug_log()` | debug 用。trace **預設關閉**，開啟後只寫進 300 行環狀緩衝（`get_debug_log()` 讀），不印 stdout；前端由 Tab 連動除錯面板一起開關。 |

**內部關鍵函數**（private）：

| 函數 | 做什麼 |
|------|--------|
| `setup_world()` / `setup_map()` | 開局：生成地圖、放英雄與 NPC、註冊進引擎。 |
| `make_ctx()` | 組出 `EngineCtx`（綁好 decide/resolve/trace callback）給 `step()` 用。 |
| `pump()` | **驅動回合**：反覆 `step()`，遇玩家等待/回合結束/換樓層就停。 |
| `drain_events()` | 把 `events_` 翻成 Godot signal；回傳「英雄是否踩到下樓梯」。 |
| `npc_decide(e)` | NPC 決策入口（接 `decide_npc`，共用橋樑層的 `rng_`）。 |
| `next_floor()` | 換下一層：先套用下樓成長（英雄 `+HERO_MAXHP_PER_FLOOR` 最大 HP、`+HERO_HEAL_PER_FLOOR` 治療、`+HERO_ATK_PER_FLOOR` 攻擊，發 `hero_grew`），再 `setup_map()` 重建樓層。沒有這個成長，怪物逐層變強而英雄不變，第 5 層會變成數學上打不贏——`WIN_FLOOR=5` 因此仰賴這個機制才可能達成。 |
| `recompute_fov()` | 重算英雄視野。 |
| `add_actor_to_engine(e)` | 把實體加進回合引擎。 |

**對前端發出的 signal**：

| signal | 何時發出 |
|--------|----------|
| `world_changed` | 畫面該刷新（每次 `pump()` 停下時）。 |
| `round_finished` | 一輪所有 actor 都跑完。 |
| `floor_changed(floor_num)` | 換到新樓層。 |
| `hero_grew(max_hp, attack)` | 下樓成長生效後（新的最大 HP／攻擊力）。 |
| `hero_bumped_wall` | 英雄撞牆。 |
| `hero_attacked(damage, target_hp_left)` | 英雄打中目標但未致死。（取代舊版 `hero_bumped_npc(String)`。） |
| `hero_hurt(damage, hp_left)` | 英雄被 NPC 打中但未死亡。**新增**——舊版玩家被打時完全沒有回饋，這是修掉的一個真 bug。 |
| `npc_died()` | 有 NPC 死亡（無參數；取代舊版 `npc_died(String)`）。 |
| `item_picked_up(item_name, heal_amount)` | 撿到道具。 |
| `game_over` | 英雄死亡。 |
| `game_won(floor_num)` | 在 `get_win_floor()` 那層走下樓梯，通關。**新增**。 |

---

## 5. 前端 `godot_zone/`

- `map_view.tscn` / `map_view.gd` — 主場景：`DungeonView` + `Camera2D(zoom 2)` + `HUD`（`Stats`／`HPBar+HPText`／`MessageLog`／`DebugPanel`／死亡&勝利 `Overlay`）。收按鍵（WASD/方向鍵/數字鍵移動、`.`/KP5 等待、R 重開、F5/F9 存讀檔、Tab 開關除錯面板）→ 呼叫 `ZoneWorld`；接 signal → 更新 HUD／訊息列／結局 overlay。**不決定畫面上的 tile 或顏色**，全部走訊息列而非 `print()`。
- `dungeon_view.gd` — 地城繪製層：`_draw()` 逐格從 `assets/tilesheet.png` 取圖塊畫地形與實體，資料來源是 `ZoneWorld::get_tile_flags()` / `get_visible_entities()`；霧中戰爭＝同圖塊調暗＋偏冷色，不用 shader。**唯一決定「畫哪個 tile、什麼顏色」的地方**。
- `assets/tilesheet.png` + `LICENSE-kenney-1bit.txt` — Kenney 1-Bit Pack（CC0），16×16 圖塊、49 欄、無間距、單色可任意上色。
- `verify.gd` — headless 端到端驗證腳本（跑出 `VERIFY PASSED`）。除基本欄位檢查外，`_run_bot()` 用固定種子（`BOT_SEED=12345`）跑 BFS 尋路機器人一路殺向樓梯，斷言遊戲必達結局（通關或戰死）、至少到第 2 層、且過程中英雄有掉血——這取代了舊版「英雄站著不動、NPC 必須找到並打中他」的斷言（LOS-based AI 下該假設不成立）。
- `bin/` — 編譯產物 `.so`（gitignore，建置時自動複製過來）。

---

## 6. 測試 `tests/`

doctest 單元測試（33 case / 129 assertion 全綠，`./build/bin/zone_test` 實跑確認）：
- `test_turn_engine.cpp` — 加入序＝回合序、act_point 重設、玩家阻塞 submit 後續跑、撞擊致死發 `ActorDied`。
- `test_npc_combat.cpp` — 測 `decide_npc` + `apply_action`（8 case）：警覺鄰接攻擊、target 由呼叫端傳入不受實體建立順序影響、`move_chance=0` 仍必定攻擊、`HeroComponent` 序列化後仍可辨識、從未看見英雄不會憑空警覺、警覺記憶（`alert_memory`）到期解除、直線追擊被牆擋住時改走另一軸不卡死、徘徊時四周被佔滿則等待不誤傷同類。
- `test_serialize.cpp` / `test_ecs.cpp` / `test_phase4.cpp` / `smoke_test.cpp` — 存讀檔、ECS、其他。

---

## 7. 一句話記住每層

- **core**：規則與資料，純 C++，可單測。
- **gbind / ZoneWorld**：翻譯官，唯一碰 Godot 的地方。
- **godot_zone**：畫面與輸入。
- **TurnEngine + apply_action**：回合制的兩個主角——一個管「輪到誰」，一個管「動作的結果」。
