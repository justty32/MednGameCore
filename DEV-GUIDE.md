# DEV-GUIDE — 結構整理參考

這是被動參考，只在重構、拆檔、整理資料夾、長出新子工作流時取用。日常先走 [WORKFLOWS.md](WORKFLOWS.md)。

## 結構整理原則

兩個觸發：

- 單體過大：某檔案、工作流入口、code 大檔、文檔大到難讀。
- 資料夾雜亂：不同用途的檔案混在一起，難導航。

處理方法：

1. 按內容、職責、用途語意分，不硬切等分。
2. 放進專屬子資料夾或既有領域檔。
3. 子資料夾頂層留 README 或 INDEX 做導航。
4. 原本位置只留精簡指標和連結，不塞細節。

## 建議門檻

- `src/`、`examples/`：單檔超過 300 行就檢視是否拆。
- `docs/` 使用手冊：單檔超過 300 行就檢視是否拆。
- `workflows/` 操作筆記：單檔超過 8192 bytes 就檢視是否拆。

門檻是檢視訊號，不是硬性上限；本質完整的 spec/plan 可以保留，落地後移到 `archive/` 凍結。

## 工作流成長軌跡

1. 單檔：例如 `testing.md`。
2. 資料夾 + 單 README：入口兼導引與 index。
3. 資料夾內不只 README：拆出 `session-log.md`、`gotchas.md`、內容檔。
4. 資料夾內有子工作流：各子工作流自己從第 1 級開始長。

升級和降級都依「是否膨脹/雜亂」決定，不預先過度設計。

## 跳流程規則

工作流只在能**降低交接、同步或設計風險**時啟用；否則小改直接做。可以跳過完整 workflow 的情況：

- 單行或小範圍修正，風險低且不跨 session。
- 純查詢或一次性回答，不會留下 durable 知識。
- 使用者明確要求快速處理，不需要文檔化。
- 既有工作流只會增加同步成本、不會降低風險。

跳流程不代表跳過基本工程規矩：仍要讀必要上下文、避免破壞使用者改動、能測就測。

## Done when 細則

每個非微小工作先寫一句可驗證的完成定義：`Done when: <可觀察、可驗證的完成條件>`。好的完成定義——

- 有可驗證結果：測試、文件、索引、可執行 demo、或使用者驗收。
- 說清楚哪些事**不**包含在本次工作。
- 能讓下一個 agent 判斷「該停了」。

> Truth Layers（優先級）與多 Agent 並行的細則在 [workflows/common/conventions.md](workflows/common/conventions.md)。

